import 'package:flutter/material.dart';

class IngredientsListPage extends StatelessWidget {
  const IngredientsListPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("plop");
  }
}